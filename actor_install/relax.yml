#example
---
SOURCES:
  - VCS:  git
    REPO: ssh://git@git.iter.org/heat/relax.git
    VERSION: develop
    DIR: relax

MODULES: [IMAS-AL-Fortran, IMAS-AL-Python, iWrap, XMLlib]

BUILDS: 
  - DIR: relax
    CMD:
      - cd lib/nag && FC=ifort ./naglib.mk
      - cd lib/netlib && FC=ifort ./netlib.mk
      - cd lib/ppplib && FC=ifort ./ppplib.mk
      - FC=ifort make -f make/makefile_imas clean librelax.a 

ACTORS: 
  - DIR: relax
    CMD: make -f make/makefile_imas clean actor FC=ifort

#COMMENTS:  
...
  
